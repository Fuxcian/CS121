package week10;

public class ArrayListDemo {
    //What is an array list?
    /* It implements the list collection, which is the data structure (container)
     * which stores a collection of data
     */
    /* In Java an arrayList is a part of the Java collection Framework.
     * It is in the java.util package
     * - resizable array (grow/shrink)
     * - intern
     */
    // ArrayList<String> cakeInfo = new ArrayList<String>();
    // How to declare other variables:
    /* ArrayList<Integer> nums = new ArrayList<Integer>();
     * ArrayList<Doubles> dubs = new ArrayList<Double>();
     * ...etc
     */
    /* Methods with an ArrayList collection
     * .add() --> nums.add(4);
     * .get() --> nums.get(0);
     * .remove() --> nums.remove(3);
     * .size() --> nums.size();
     */
}
